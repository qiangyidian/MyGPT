# 生产 API 基址 — Next.js 在 `next build` 时内联 NEXT_PUBLIC_*，
# 不带此变量的构建会回退到 http://localhost:8000，线上表现为登录 Failed to fetch
# （HTTPS 页面请求 HTTP localhost 被 mixed-content 拦截）。
# 显式 shell 环境变量优先级高于本文件，deploy 脚本中的传值不受影响。
NEXT_PUBLIC_API_BASE_URL=https://mychat.qiangi.top
